name: test

on:
  pull_request:
  workflow_dispatch:

permissions:
  checks: write
  pull-requests: write

jobs:
  ios-sdk-release-test:
    runs-on: macos-26
    strategy:
      fail-fast: false
      matrix:
        include:
          - xcode-version: '26.3'
            simulator-os: '26.2'
          - xcode-version: '26.6'
            simulator-os: '26.5'
    steps:
      - uses: actions/checkout@v3

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ matrix.xcode-version }}

      - name: Install xcbeautify
        run: brew install xcbeautify

      - name: Prebuild before test # 테스트 전에 빌드
        run: |
            set -o pipefail && xcodebuild build-for-testing \
                -project Hackle.xcodeproj \
                -scheme Hackle-Release \
                -destination 'platform=iOS Simulator,name=iPhone 17,OS=${{ matrix.simulator-os }}' \
                -derivedDataPath "$RUNNER_TEMP/DerivedData" \
                | xcbeautify --renderer github-actions --quiet

      - name: Verify framework resources
        run: |
            FRAMEWORK_PATH="$RUNNER_TEMP/DerivedData/Build/Products/Release-iphonesimulator/Hackle.framework"
            if [[ ! -d "$FRAMEWORK_PATH" ]]; then
                echo "::error::Hackle.framework not found at $FRAMEWORK_PATH"
                exit 1
            fi

            missing=0
            while IFS= read -r resource; do
                file_name="$(basename "$resource")"
                if [[ ! -f "$FRAMEWORK_PATH/$file_name" ]]; then
                    echo "::error file=$resource::Missing framework resource: $file_name"
                    missing=1
                fi
            done < <(find Sources/Hackle/Resources -type f | sort)

            exit "$missing"

      - name: Test
        run: |
            set -o pipefail && xcodebuild test-without-building \
                -project Hackle.xcodeproj \
                -scheme Hackle-Release \
                -destination 'platform=iOS Simulator,name=iPhone 17,OS=${{ matrix.simulator-os }}' \
                -derivedDataPath "$RUNNER_TEMP/DerivedData" \
                -retry-tests-on-failure \
                -test-iterations 3 \
                -parallel-testing-enabled NO \
                -resultBundlePath TestResults \
                | xcbeautify --renderer github-actions --quiet --report junit

      - name: Publish Test Report
        uses: EnricoMi/publish-unit-test-result-action/macos@v2
        if: success() || failure()
        with:
          files: build/reports/junit.xml
          check_name: Test Results (Xcode ${{ matrix.xcode-version }})
          comment_title: Test Results (Xcode ${{ matrix.xcode-version }})
          comment_mode: changes
